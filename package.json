{
  "name": "azulito-template",
  "version": "1.0.0",
  "description": "Azulito - A modern portfolio website template",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir dist",
    "serve": "npx serve dist -p 3000",
    "preview": "npm run build && npm run serve"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "html-minifier-terser": "^7.2.0",
    "sharp": "^0.34.5",
    "svgo": "^4.0.0"
  }
}