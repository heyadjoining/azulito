// Page content modules for SPA routing
// Individual pages are loaded from separate files for easier editing

const pages = {
    home: homePage,
    work: workPage,
    story: storyPage,
    blog: resourcesPage,
    chat: chatPage
};
